# Decision Playbook

This project provides an interactive playbook for decision making and learning. The frontend is a single page application served from `public/index.html`.

## Running locally

Install dependencies and start the server:

```bash
npm install
npm start
```

The app will be available at [http://localhost:3000](http://localhost:3000).
